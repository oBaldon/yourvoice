PORT=3000
# Room key opcional (se ficar {{PLACEHOLDER}}, não exige chave)
ROOM_KEY={{PLACEHOLDER}}

# mediasoup RTC port range (mapeado no docker-compose)
RTC_MIN_PORT=40000
RTC_MAX_PORT=40100

# IMPORTANTE: IP "anunciado" pelo servidor nos candidatos ICE.
# Em VPN (Tailscale/ZeroTier/Hamachi), use o IP da VPN DO HOST (máquina que roda o Docker).
# Exemplo Tailscale: 100.64.0.10
ANNOUNCED_IP={{PLACEHOLDER}}